{
  "name": "통합 영업 분석 대시보드",
  "install": "bash .cursor/install.sh",
  "terminals": [
    {
      "name": "streamlit",
      "command": "source .venv/bin/activate && streamlit run app.py --server.port=8501 --server.address=0.0.0.0 --server.headless=true",
      "description": "메인 영업 분석 대시보드 (http://localhost:8501)"
    }
  ],
  "ports": [
    {
      "name": "dashboard",
      "port": 8501
    }
  ]
}
