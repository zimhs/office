# 맥·아이패드 공용 Streamlit Cloud URL
# (앱 secrets에 dashboard_cloud_url 이 있으면 그게 우선)
dashboard_cloud_url = "https://office-g8ryabkapprkpjmfwa5aypw.streamlit.app"

# 로컬 Streamlit ↔ Streamlit Cloud 업무일지 양방향 동기화 (GitHub Secret Gist)
# 1) GitHub PAT (gist 권한) — 로컬·Cloud 양쪽에 동일하게
# 2) worklog_gist_id — 한쪽에서 첫 저장 후 화면에 나온 id를 양쪽에 넣기
#    (비우면 토큰만으로 첫 저장 시 자동 생성)
github_token = "ghp_xxxxxxxx"
worklog_gist_id = ""
dashboard_cache_gist_id = ""

# 단가인상 탭 — 다음메일 SMTP 자동발송
# 다음메일: 설정 → POP/IMAP 설정 → 「POP3/IMAP 사용」켜기
smtp_provider = "daum"
smtp_host = "smtp.daum.net"
smtp_port = 465
smtp_user = "아이디@daum.net"
smtp_password = "메일비밀번호"
smtp_from_name = "신일가스"
